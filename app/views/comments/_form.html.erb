<%= form_for([@user, @user.comments .build]) do |f| %>
  <!--
  <p>
    %= f.label :Tweet %><br>
    %= f.text_field :text %>
  </p>
  -->
  <p>
    <!--%= f.label :body %>-->
    <br>
    <%= f.text_area  :body , :size => "70x5" %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>