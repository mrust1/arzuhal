<hr/>
<div>
<table>
    <tr>
    <th> İMAGE</th>
    <th> <%= @user.name %>  </th>
    <th>Email : <%= @user.email %> </th>
    <th>Join Date : <%= display_turkish_time(@user.created_at) %></th>
  </tr>
<div style="width:600px">
    <% @users.each do |user| %>
      <tr >
           <td COLSPAN=4><%= user.body %></td>
         </tr>
         <% end %>
</div>
</table>
<!--
</div>
<p>
  <strong>Name:</strong>
  %= @user.name %>
</p>
<p>
  <strong>Surname:</strong>
  %= @user.surname %>
</p>
<p>
  <strong>Email:</strong>
  %= @user.email %>
</p>
<p>
  <strong>Body:</strong>
  %= @user.body %>
</p>
-->

<h2>Add a Tweet:</h2>
<%= render "comments/form" %>
<h2>Add new Tweet this user</h2>
<hr/>
<%= render @user.comments %>

<%= link_to 'Edit User', edit_user_path(@user) %> |
<%= link_to 'Back to Users', users_path %>